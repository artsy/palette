import React from "react"
import { Ribbon } from "./Ribbon"

export default {
  title: "Components/Ribbon",
}

export const Default = () => {
  return <Ribbon ribbonText="Black Owned" repetitionCount={6} />
}
