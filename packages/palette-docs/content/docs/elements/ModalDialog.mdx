---
name: ModalDialog
source: elements/ModalDialog
---

### Default

<Playground>
  <State initial={{ show: false }}>
    {({ state, setState }) => (
      <>
        <Button onClick={() => setState({ show: true })}>Show</Button>
        {state.show && (
          <ModalDialog onClose={() => setState({ show: false })}>
            <Text variant="sm">Some example content</Text>
          </ModalDialog>
        )}
      </>
    )}
  </State>
</Playground>

### With Title

<Playground>
  <State initial={{ show: false }}>
    {({ state, setState }) => (
      <>
        <Button onClick={() => setState({ show: true })}>Show</Button>
        {state.show && (
          <ModalDialog
            title="Modal Title"
            onClose={() => setState({ show: false })}
          >
            <Text variant="sm">Some example content</Text>
          </ModalDialog>
        )}
      </>
    )}
  </State>
</Playground>

### With Logo

<Playground>
  <State initial={{ show: false }}>
    {({ state, setState }) => (
      <>
        <Button onClick={() => setState({ show: true })}>Show</Button>
        {state.show && (
          <ModalDialog
            hasLogo
            title="Modal Title"
            onClose={() => setState({ show: false })}
          >
            <Text variant="sm">Some example content</Text>
          </ModalDialog>
        )}
      </>
    )}
  </State>
</Playground>

### With Footer

<Playground>
  <State initial={{ show: false }}>
    {({ state, setState }) => (
      <>
        <Button onClick={() => setState({ show: true })}>Show</Button>
        {state.show && (
          <ModalDialog
            title="Modal Title"
            onClose={() => setState({ show: false })}
            footer={<Button width="100%">Click me</Button>}
          >
            <Text variant="sm">Some example content</Text>
          </ModalDialog>
        )}
      </>
    )}
  </State>
</Playground>

### Custom Width

<Playground>
  <State initial={{ show: false }}>
    {({ state, setState }) => (
      <>
        <Button onClick={() => setState({ show: true })}>Show</Button>
        {state.show && (
          <ModalDialog
            width={["100%", 800]}
            onClose={() => setState({ show: false })}
          >
            <Text variant="sm">Some example content</Text>
          </ModalDialog>
        )}
      </>
    )}
  </State>
</Playground>
