---
name: Stepper
---

Used when users need a sense of upcoming actions. User can go back to tabs they
have completed, but cannot go forward until completeing.

<Playground>
  <Stepper initialTabIndex={2} currentStepIndex={2} disableNavigation={false}>
    <Step name="Review" />
    <Step name="Confirm" />
    <Step name="Pay" />
  </Stepper>
</Playground>
