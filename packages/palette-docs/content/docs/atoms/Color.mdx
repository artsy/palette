---
name: Color
---


The Artsy color palette has been updated to improve the accessibility. Each color has been adjusted to ensure sufficient contrast and has specific roles as to which colors can be used in conjunction with each other.

<Spacer m={2} />

<Flex flexWrap='wrap'>
	{Object.entries(THEME_V3.colors).map(([key, value]) => {
		return (
			<Box pr={2} pb={2}>
				<ColorComponent color={key} />
			</Box>
		)
	})}
</Flex>
