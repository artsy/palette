---
name: Theme
source: Theme.tsx
---

The `<Theme>` component is our main Palette context provider. Pass it a
`theme="v2 | v3"` prop to set the theme version.

### Usage

```tsx
import { Theme } from "@artsy/palette"

export const App = (props) => {
  return <Theme theme="v3">...</Theme>
}
```
