---
name: getThemeConfig
source: Theme.tsx
---

The `getThemeConfig` function will return a theme object by version id (`2` or
`3`). Useful for outside of a React component context.

### Usage

```tsx
import { getThemeConfig } from "@artsy/palette"

const { space } = getThemeConfig({ id: "3" })
```
