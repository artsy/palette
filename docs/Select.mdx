---
name: Select
---

import { Playground, PropsTable } from 'docz'
import { LargeSelect, SmallSelect } from '../src'
import { Value } from 'react-powerplug'

# Select

## LargeSelect

<Playground>
  <Value>
    {({ value, set }) => (
      <LargeSelect
        options={[
          {
            text: "Most Recent",
            value: "mostRecent",
          },
          {
            text: "Price",
            value: "price",
          },
          {
            text: "Estimate",
            value: "estimate",
          },
        ]}
        selected={value}
        onSelect={value => set(value)}
      />
    )}
  </Value>
</Playground>

## SmallSelect

<Playground>
  <Value>
    {({ value, set }) => (
      <SmallSelect
        options={[
          {
            text: "Most Recent",
            value: "mostRecent",
          },
          {
            text: "Price",
            value: "price",
          },
          {
            text: "Estimate",
            value: "estimate",
          },
        ]}
        selected={value}
        onSelect={value => set(value)}
      />
    )}
  </Value>
</Playground>
