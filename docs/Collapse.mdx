---
name: Collapse
---

import { Playground, PropsTable } from 'docz'
import { Toggle } from "react-powerplug"
import { Collapse, BorderBox, Button } from '../src'

# Collapse

## Open

<Playground>
    <Collapse open>
      <BorderBox>
        A Collapse component ...
        Not really cool yet!
      </BorderBox>

    </Collapse>
</Playground>

## Toggle

<Playground>
  <Toggle>
    {({ on, toggle }) => (
      <>
        <Button onClick={toggle}>{on ? 'Close' : 'Open'}</Button>
        <Collapse open={on}>
          <BorderBox my={2}>
            A Collapse component with toggle button ...
            It's pretty cool!
          </BorderBox>
        </Collapse>
      </>
    )}
  </Toggle>
</Playground>

<PropsTable of={Collapse} />
