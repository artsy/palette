---
name: BorderBox
---

import { Playground, PropsTable } from 'docz'
import { BorderBox, Message, Sans, Spacer } from '../'

# BorderBox

<Playground>
  <BorderBox hover>
    A BorderBox component is a container for content that exposes props for space,
    positioning, size and more, which optionally takes a `hover` prop for highlighting
    content.
  </BorderBox>
</Playground>

<PropsTable of={BorderBox} />
