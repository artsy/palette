---
name: New Icons
status: wip
---

import { NewIcons } from "@artsy/palette"
import _ from "lodash"

See [Notion](https://www.notion.so/artsy/Icons-c49c292a40e243baacd7842a4de983d4)
for complete list.

<Spacer my={4} />

<Flex flexWrap="wrap">
  {Object.entries(NewIcons).map(([iconName, IconComponent], index) => {
    const blocklist = ["Icon"]
    if (
      !_.isFunction(IconComponent) ||
      blocklist.some(icon => icon === iconName)
    ) {
      return null
    }
    const size = "40px"
    return (
      <Box pr={4} pb={2} mb={4} key={index}>
        <IconComponent width={size} height={size} />
        <Separator my={1} />
        <Sans size="3" weight="medium">
          {iconName}
        </Sans>
      </Box>
    )
  })}
</Flex>
